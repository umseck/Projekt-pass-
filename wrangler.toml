name = "projektpass-pilot"
compatibility_date = "2026-09-23"
pages_build_output_dir = "public"
# Configure APP_ORIGIN, SUPABASE_URL and SUPABASE_PUBLISHABLE_KEY in Pages.
# SUPABASE_SECRET_KEY must be an encrypted secret, never a public build variable.
# Use a separate Supabase project for preview; do not share production secrets.
